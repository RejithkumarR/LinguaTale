spring:
  application:
    name: linguatale-api

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

linguatale:
  ai:
    base-url: ${LINGUATALE_AI_BASE_URL:http://localhost:8000}
